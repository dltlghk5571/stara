# 이 파일을 복사해서 .env로 만들고 실제 값을 채우세요.
#   cp .env.example .env
# .env는 .gitignore에 포함되어 있어서 git에 올라가지 않습니다.

# Map-It과 공유하는 Kakao Developers 앱의 REST API 키.
# developers.kakao.com > 내 애플리케이션 > (Map-It용 앱) > 앱 키 탭 에서 복사.
# (JS 키가 아니라 REST API 키 — 서버사이드 배치 스크립트용)
KAKAO_REST_API_KEY=
