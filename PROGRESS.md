# STARA 진행 기록

K-pop 아티스트의 발자취를 따라가는 게임형 서울 여행 코스 앱. Next.js 16(App Router) 기반, Vercel 배포.

- 프로덕션: https://stara-red.vercel.app
- 저장소: dltlghk5571/stara (GitHub)

---

## 현재 시스템 구성

### 스택
| 영역 | 선택 | 비고 |
|---|---|---|
| 프레임워크 | Next.js 16 (App Router, Turbopack) | |
| 배포 | Vercel | GitHub 연동, `main` 푸시 시 자동 배포 |
| 인증 | Clerk | 현재 dev 키 — production 전환은 보류 중 |
| DB | Neon Postgres (Drizzle ORM) | `users`, `quest_photos` 2개 테이블 |
| 사진 저장 | Vercel Blob (public) | 클라이언트 직접 업로드 |
| 지도 | TMap JS SDK (Tmapv2) | 기존 Leaflet/OpenStreetMap에서 전면 교체 |
| 길찾기 | TMap 길찾기 API (서버) + Haversine 폴백 | |
| 관광 정보 | 한국관광공사 TourAPI | |
| 클라이언트 상태 | Zustand | 여행 진행 상태(`tripStore`), 파생 데이터(`useTripPlan`) |
| 테스트 | Vitest | 39개 테스트, 순수 로직 위주 |

### 핵심 기능
- **온보딩**(로그인 필수 → 아티스트 선택 → 지역 선택 → TourAPI 3안 생성): 서울·인천·부산 지역 지원, 나머지 5개 지역은 준비중 표시
- **여행 앱 셸**(`/trip`): Cover·Stamps·Route·Diary 하단 탭 하나로 통합된 SPA — 지도에서 경로 확인, 코스 편집(`/edit`)
- 체크포인트 미션: **사진 첨부가 필수**, 제출 시 업로드+퀘스트완료+스탬프획득이 한 번에 처리됨
- **다이어리 탭**: 체크포인트 인증샷을 날짜별로 모아 보여주는 스크랩북, 루트(`tripId`)별로 탭 구분
- **컬렉션북**(`/collection/[username]`): 공개 페이지, 인스타그램 공유용 카드 이미지 자동 합성 (다이어리 탭과 별개, 계속 유지)
- 지도에 GPS 현재 위치 표시 (동의 기반)

---

## 지난 주 (8/12 ~ 8/14)

### 8/13 — 실제 데이터 연동
- 한국관광공사 TourAPI, TMap 길찾기 API 연동 (`5b91635`)
- 캐싱, 관광지 랭킹 시그널, 연관 관광지 추천 로직 추가

### 8/14 — 대규모 기능 확장 (하루에 인증/DB/사진/컬렉션북/지도엔진까지)
1. **백엔드 기반 마련**: Vercel 마켓플레이스로 Clerk(인증)·Neon(DB)·Blob(파일 저장) 프로비저닝, DB 스키마 설계 (`7aab7ef`)
2. **퀘스트 인증샷**: 체크포인트 완료 시 사진 첨부, Blob 클라이언트 직접 업로드 (`8c17d29`)
3. **컬렉션북 + 인스타 공유**: 유저네임 클레임, `next/og`로 9:16 카드 이미지 합성, Web Share API 공유 (`4d8c70a`)
4. **GPS 위치 표시**: 동의 기반 현재 위치 마커 (`89c46a0`)
5. **지도 엔진 전면 교체**: react-leaflet/OpenStreetMap → TMap 자체 JS SDK (`9802e46`)
   - TMap이 타일 URL을 공개하지 않아 SDK 통째로 새로 구현 필요했음
   - SDK가 `document.write()`에 의존하는 구식 구조라, 동적 스크립트 주입(`next/script` 포함)으로는 초기화가 깨지는 문제를 발견 → 루트 레이아웃에 일반 `<script>` 태그로 직접 삽입해 해결
6. **마무리**: OG 프리뷰, 컬렉션 갤러리, 유저네임 금칙어, 전 화면 로그인 진입점 추가, 사진 업로드/컬렉션북 첫 실사용 버그 수정 (`dc8b628` ~ `88690e1`)

---

## 이번 주 (8/17)

### 실사용 테스트에서 나온 버그 수정
- **사진 업로드 실패**: 아이폰 카메라 기본 포맷(HEIC)을 서버가 거부하고 있었음. `capture` 속성을 제거해 iOS가 사진을 넘기기 전 자체적으로 JPEG 변환하도록 수정, 서버도 `image/*` 전체 허용
- **컬렉션북 카드 이미지 500 에러** — 두 가지 원인이 겹쳐 있었음
  1. HEIC 사진이 포함되면 `next/og`(Satori)가 디코딩 실패로 전체 카드를 깨뜨림 → 합성 전 필터링
  2. Satori는 자식 노드 2개 이상인 `<div>`에 `display: flex`를 명시해야 하는데 이름 표시 부분에 빠져있었음 → 사진 유무와 무관하게 항상 실패하던 별개의 버그, 수정 후 로컬에서 실제 사진 넣고 렌더링 확인 완료
- **사진 저장 시 외래키 위반**: `quest_photos`가 `users`를 참조하는데, 유저네임을 먼저 안 정하면 로컬 유저 행이 없어서 저장이 막혔음 → 사진 저장 시점에 유저 행 자동 생성, 이름은 로그인 이메일 앞부분에서 유도
- **로그인 진입점 부재**: 메인/편집/최종확인 등 어디에도 로그인 버튼이 없었음 → 모든 주요 화면 상단에 로그인/컬렉션북 바로가기 추가
- **완료 화면의 죽은 안내 문구**: "컬렉션북 기능은 추후 제공됩니다" 플레이스홀더가 실제 기능을 만든 뒤에도 안 지워져 있었음 → 실제 링크로 교체

---

## 이번 주 (8/20) — 프론트엔드 팀 프로토타입 통합

프론트엔드 팀이 넘긴 정적 프로토타입(`stara_full_prototype_en.html`)을 기준으로 기존 화면을 전부 새 디자인으로 갈아엎음. Fraunces/Plus Jakarta Sans/Caveat/Space Mono 폰트, 코랄·네이비·민트 팔레트 도입.

### 온보딩 플로우 신규 구축 (`85ee242`)
- 로그인을 스플래시 다음 필수 관문으로 변경(`proxy.ts` 미들웨어, 기존엔 로그인 없이도 전부 이용 가능했음)
- 아티스트 선택(`/onboarding/artists`) → 지역 지도/상세(`/onboarding/region[/regionId]`, 서울·인천·부산만 활성) → 메인 루트 3안 생성(`/onboarding/generate`)
- 3안은 아티스트에 의존하지 않고 TourAPI 카테고리(관광지/문화시설/음식점)로 뽑음 — 새 API 라우트 없이 기존 `/api/tourism/nearby`를 재사용
- 설계 도중 발견: `MAIN_ROUTE_PLACE_IDS`(서울 고정 루트)가 `autoPlaceSelector.ts`에 하드코딩되어 있어, 온보딩에서 고른 루트가 실제로 메인 루트를 대체하도록 파라미터화 필요 → `tripStore.mainRoutePlaces`로 동적화

### 버그 수정 (`f6ca33c`, `ec5325b`)
- 홈 화면이 새 디자인과 안 맞는 예전 화면 그대로였던 것 → 프로토타입 스플래시 화면으로 교체
- `/main-route`가 `useTripPlan`을 거치지 않고 독자적으로 서울 고정 루트를 계산하던 버그 — 온보딩에서 고른 루트가 `/edit`에는 반영되는데 `/main-route`엔 안 되는 현상으로 발견됨
- 출발시간만 설정 가능하던 것 → 종료시간도 설정 가능하게(`tripStore.tripEndTime`)
- (기존 버그) TourAPI로 자동 보완된 장소는 퀘스트가 0개로 잡혀 스탬프가 조건 없이 통과되던 문제 발견 및 수정

### 앱 전체를 새 프론트로 교체 (`7641017`) — 가장 큰 작업
`/main-route → /edit → /final → /travel → /stamps`로 흩어져 있던 여행 진행 화면을 프로토타입의 하단 탭 구조(Cover/Stamps/Route/Diary) 하나(`/trip`)로 통합. `/final`, `/travel`, `/stamps`, `/main-route` 페이지 자체를 삭제.
- **DB 스키마 변경**: `quest_photos`에 `trip_id`/`trip_name` 컬럼 추가(별도 trips 테이블 없이, 다이어리 탭에서 루트별로 사진을 묶어 보여주기 위함) — `drizzle-kit push`로 반영
- **MissionSheet 신규**: 사진 첨부 → 업로드 → 퀘스트 완료 → 스탬프 획득을 하나의 필수 흐름으로 묶음(기존엔 사진 업로드와 퀘스트 체크가 서로 완전히 무관해서, 사진 없이도 체크만으로 스탬프를 받을 수 있었음)
- 지도 핀에 진행 상태(완료/다음/잠김) 표시 추가
- 프로토타입의 "지도 탭해서 직접 핀 찍기" 나만의 루트 기능은 새로 구현하지 않고, 기존 `/edit`(카테고리·아티스트 필터) 화면을 리스타일해서 대체 — 지도 클릭 이벤트·임의 좌표 저장 인프라가 아예 없어서 새로 만들면 작업량이 컸음
- 다이어리의 "봉투 열람(N시간 후 공개)" 연출은 프로토타입에서도 실제 카운트다운 없는 정적 텍스트였어서 이번엔 뺌 — 미션 완료 즉시 다이어리에 반영되도록 함

---

## 알려진 이슈 / 다음에 할 일

- **TMap 클라이언트 키 무방비 노출** — SK Open API가 도메인/리퍼러 제한을 지원하지 않음(IP 제한만 있음, 브라우저 앱엔 무의미). 사용량 모니터링 외 뾰족한 대응 수단 없음, 보류 중
- **Clerk production 키 미전환** — 커스텀 도메인 정할 때 같이 진행하기로 함
- 기존에 저장된 HEIC 사진은 여전히 브라우저 대부분에서 안 보임 (근본 수정은 신규 업로드부터 적용됨)
- **인천·부산은 STARA 자체 촬영지 데이터가 아직 없음** — 전부 TourAPI 실시간 조회로만 구동, 대표 아티스트 카드도 없음(TourAPI 인기 스팟 미리보기로만 대체). 실 데이터 수집되는 대로 서울처럼 전환 필요
- **`/edit` 화면의 하위 컴포넌트**(FilterBar/ReelCard/ScheduleFooter 등)는 이번에 시각적으로 새 디자인 언어를 안 입힘 — 페이지 톤만 맞추고 나머지는 기존 그대로라 톤이 살짝 안 맞음
- Route 탭의 경로선은 진행 상태에 따라 구간별로 색이 안 바뀜(전체 단색) — 프로토타입처럼 완료 구간만 실선/색칠하려면 TMap 폴리라인을 구간별로 쪼개야 해서 이번 범위엔 안 넣음
