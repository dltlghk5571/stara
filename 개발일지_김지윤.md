케이팝 아이돌 장소 크롤링 작업정리

1. 크롤링 대상 사이트 및 선정 이유  
* HaniSeoul (haniseoul.com): 서울 K-pop 성지순례 코스 정리 사이트. 기획사 공식 시설 \+ 촬영지 정보를 한 번에 다룸  
* 한류투어 (hallyu-tour.com): 특정 그룹(세븐틴) 단독 맛집 정리 사이트. 그룹 단위로 좁혀진 데이터 테스트용  
* Visit Seoul (visitseoul.net): 서울관광재단 공식 관광정보 사이트. 회의에서 정한 "관광 관련 사이트" 기준에 가장 부합. 주소·운영시간까지 구조화되어 있어 address 필드 실제 추출 가능  
* 티스토리/네이버 블로그는 검색으로 URL을 직접 찾기 어려워서 이번 1차 크롤링에는 미포함. 브라우저로 직접 검색해서 urls 리스트에 추가하는 방식으로 확장 예정

2. 코드가 하는 일 (전체 흐름)  
   urls 리스트 → 각 URL 요청해서 HTML 받기 → 도메인별로 알맞은 파서 함수 선택 → 장소명+설명(+주소) 추출 → 미리 정리한 그룹 리스트와 대조해서 아티스트 자동 매칭 → 매칭된 아티스트 수만큼 결과 분리 → 같은 장소끼리 중복 병합 → candidates.json으로 저장  
     
3. 핵심 설계: 사이트마다 다른 파서  
   사이트마다 HTML 구조가 달라서 하나의 코드로 다 처리 불가함. 실제로 3개 사이트 테스트하면서 아래 차이 확인함  
* HaniSeoul: 장소명이 li 안의 strong 태그. 설명은 같은 li 안 나머지 텍스트  
* 한류투어: 장소명이 h2 소제목 안의 b 태그. 설명은 다음 h2 나오기 전까지의 p 태그들  
* Visit Seoul: 장소명이 h4 태그 자체. 설명 문단에서 "주소 : ..." 패턴을 정규식으로 찾아서 address만 따로 추출  
  그래서 parse\_strong\_style / parse\_heading\_style / parse\_visitseoul\_style 세 개 파서를 만들고, crawl\_page 함수가 URL 도메인 보고 자동으로 골라 씀

4. 아티스트 자동 매칭  
* 300만 구독자 이상 그룹 리스트를 ARTISTS라는 이름-별칭 쌍으로 코드에 저장  
* 설명 텍스트 안에 그룹명이 있는지 검사해서 매칭  
* 한 장소에 여러 그룹 언급되면 그룹 수만큼 줄 나눠서 저장  
* 매칭 안 되면 artist 빈 값으로 남겨서 사람이 확인해야 함을 표시


5. 새로 추가한 기능 (검증 필드 \+ 중복 병합)  
* verified 필드 추가: 사람이 검증했는지 여부. 기본값 False  
* direct\_mention 필드 추가: 아티스트 본인/소속사가 직접 언급한 게 맞는지 여부. 기본값 None (미확인)  
* merge\_duplicates 함수 추가: 같은 place명이면 하나로 합침. artist는 여러 개면 리스트로 모음, url도 여러 사이트에서 나왔으면 urls 리스트로 모음, address는 먼저 채워진 값 유지  
* 결과 스키마 필드명 변경: url(단일) → urls(리스트)로 바뀜  
* 중복 병합 로직 개선(문자열 유사도 기반): 처음엔 place 이름이 완전히 똑같을 때만 합쳐졌음. 실제 돌려보니 "마포 공덕 곱창파는고깃집"(haniseoul)과 "곱창파는 고깃집"(한류투어)처럼 같은 장소인데 표기가 달라서 안 합쳐지는 케이스 발견함  
  * normalize\_place 함수로 비교 전에 공백, \[운영중지\] 같은 대괄호 표시 제거  
  * 한쪽 이름이 다른 쪽에 완전히 포함되면 같은 장소로 판단 (예: "곱창파는고깃집"이 "마포공덕곱창파는고깃집" 안에 포함)  
  * 포함 관계가 아니어도 difflib로 전체 글자 유사도 계산해서 60% 이상이면 같은 장소로 판단  
  * threshold 값(현재 0.6)은 조정 가능. 너무 낮추면 다른 장소끼리 잘못 합쳐질 위험 있음

    

6. 사람이 검증해야 하는 부분 (자동화 한계)  
* "아티스트 본인/소속사가 직접 밝힌 경우"인지는 코드가 판단 못함. 텍스트에 그룹명이 등장하는지만 확인함  
* 노이즈 항목 존재함 (예: "사전 예약 안내" 같은 팁 문단이 장소로 잘못 걸리는 경우)  
* 생일카페(생카) 관련 언급은 회의에서 범위 제외로 확정했으니 섞여 있으면 제외 필요  
* place 이름에 \[운영중지\] 같은 상태 표시가 그대로 붙는 경우 있음, 정리 시 제거 필요


7. 현재까지 결과  
* 3개 사이트에서 총 23개 항목 수집 (병합 전 기준)  
* 유사도 기반 중복 병합 적용 후 최종 16개로 정리됨 (완전 일치 기준일 때는 17개였는데, "마포 공덕 곱창파는고깃집"과 "곱창파는 고깃집"처럼 표기 다른 동일 장소 1건 추가로 병합됨)   
* Visit Seoul에서 address 자동 추출 성공 사례 3건 (서울도서관, 클럽 매스, 양재천 벚꽃길)  
* 그룹 유닛 활동(MOBB \= 위너+아이콘)도 각 소속 그룹으로 정확히 매칭됨


8. 앞으로 확장 방향  
   가. 크롤링 소스 확장  
* 티스토리/네이버 블로그 URL 직접 찾아서 추가  
* 새 사이트 추가할 때마다: HTML 구조 확인 → 필요하면 파서 함수 새로 작성 → crawl\_page에 분기 추가하는 패턴 반복  
  나. 구조 자동 파악 방식 검토 (중장기)  
* 지금은 사람이 사이트 구조를 직접 확인하고 파서를 짜는 방식  
* 리스티클형 블로그는 구조가 비슷한 경우가 많아서 지금 파서 3종이 다른 사이트에도 어느 정도 재사용 가능  
* 단, 표 구조, 클래스명 기반 카드, JS 렌더링 사이트는 지금 방식으로 못 잡음  
* 대안: HTML을 LLM(AI)에 통째로 넣고 장소명/설명/주소를 추출시키는 방식. 사이트 구조 상관없이 작동하지만 API 비용, 속도, 정확도(여전히 사람 검증 필요) 트레이드오프 있음  
* 판단: 지금처럼 소수 사이트 다루는 단계에서는 맞춤 파서가 더 빠르고 무료라 합리적. 사이트 수가 크게 늘어나면(수십 개 이상) 그때 LLM 방식 전환 검토  
  다. 서비스 반영을 위해 필요한 기능 (우선순위)  
  Must have  
* 검증 상태 필드: 오늘 verified, direct\_mention 필드 추가로 1차 완료  
* 중복 병합: 오늘 완전 일치 기준 → 유사도 기반(difflib)으로 개선 완료. 표기 차이 있는 동일 장소도 병합 가능해짐  
* 지오코딩(주소→좌표): 지도/코스 기능 위해 필요. 카카오맵이나 구글 지오코딩 API 키 발급 필요  
* DB 저장: 로컬 JSON 파일 대신 실제 DB(Supabase, Firebase 등)로 이전 필요. 팀에서 어떤 DB 쓸지 먼저 결정 필요  
  Should have  
* 재크롤링 방지: 같은 URL 언제 크롤링했는지 기록해서 중복 작업 줄이기  
* 사진 URL 수집: 앱 화면에 쓸 장소 사진 필요, img 태그 src도 같이 저장  
* 관광공사 OpenAPI 매칭: 크롤링한 장소명을 공사 API에서 검색해서 공식 정보(운영시간, 입장료, 사진)와 연결  
* 영어 설명 필드: 외국인 팬 타깃이므로 번역 필드 미리 만들어두기  
  Could have  
* 관리자 검수용 화면 (구글시트/노션으로도 가능)  
* 크롤링 실패 URL 로그  
* 사이트별 크롤링 주기 설정

